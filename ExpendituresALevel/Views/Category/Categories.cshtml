@using ExpendituresALevel.Models.Category
@model IEnumerable<CategoryModel>
@{
    ViewBag.Title = "Index";
}

<h2>Categories</h2>

@{ 
    foreach(var category in Model)
    {
        Html.RenderPartial("/Views/Shared/_InfoData.cshtml", category);
    }
}

<p>
    @Html.ActionLink("Back to List", "Index")
</p>
<p>
    @Html.ActionLink("Create a category", "Create")
</p>

